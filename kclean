#!/bin/bash

#-----------------------------------------------
# httperfを停止したとき残党を殺すスクリプト
# クライアント側で使用
#-----------------------------------------------

kill -s SIGKILL `ps aux | grep httperf | grep -v 'grep' | awk '{print $2}'`
